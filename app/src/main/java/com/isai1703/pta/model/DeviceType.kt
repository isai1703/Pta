package com.isai1703.pta.model

/**
 * Tipos de dispositivo (abstracción).
 * Agrega más tipos si los necesitas.
 */
enum class DeviceType {
    ESP32,
    RASPBERRY_PI,
    ANDROID_DEVICE,
    GENERIC_HTTP,
    GENERIC_SSH,
    GENERIC_TCP,
    BLUETOOTH
}

