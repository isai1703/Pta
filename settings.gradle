pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode_PREEFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "VendingAppClienteFinal"
include(":app")

