pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode_PREEFER_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Pta"
include ':app'
